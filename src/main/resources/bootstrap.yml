spring:
  application:
    name: application
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml

logging:
  level:
    com.alibaba.nacos.client.config.impl: WARN